@model SERVOSA.SAIR.WEB.Models.TableColumnViewModel

@{ 
    int qytColumns = Model.Columns.Count;
}

<div class="containerdatatable" style="display:table-cell;">
    <table class="table table-bordered table-hover servosatable" data-tablename="@Model.TableNormalizedName">
        <thead>
            <tr>
                <th colspan="@(qytColumns + 1)">
                    <div style="display: inline-block;">
                        <p style="display: inline;">@Model.TableName</p>
                        <button data-normalizedtablename="@Model.TableNormalizedName" data-tablename="@Model.TableName" class="btn btn-default addNewColumnToVariable" type="button" style="display: inline;" title="Agregar Columna"> <span class="glyphicon glyphicon-plus"></span> </button>
                        <button data-normalizedtablename="@Model.TableNormalizedName" data-tablename="@Model.TableName" class="downloadFileForVariable btn btn-default" type="button" style="display: inline;" title="Descargar Datos de Variable"> <span class="glyphicon glyphicon-download"></span> </button>
                        <button data-normalizedtablename="@Model.TableNormalizedName" data-tablename="@Model.TableName" class="editvehicletablename btn btn-default" type="button" style="display: inline;" title="Cambiar Nombre"> <span class="glyphicon glyphicon-edit"></span> </button>
                        <button data-normalizedtablename="@Model.TableNormalizedName" data-tablename="@Model.TableName" class="removevehicletable btn btn-default" type="button" style="display: inline;" title="Eliminar Variable"> <span class="glyphicon glyphicon-trash"></span> </button>
                    </div>
                </th>
            </tr>
            <tr>
                <th data-columnname="Tareas">
                    Tareas
                </th>
                @foreach (var iColumn in Model.Columns)
                {
                    <th data-columnname="@iColumn.ColumnNormalizedName">@iColumn.ColumnName</th>
                }
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
</div>